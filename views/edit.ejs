<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Book</title>
    <link rel="stylesheet" href="../public/edit.css">
</head>
<style>
    body {
    font-family: Arial, sans-serif;
    background-color: #f0f8ff; 
    color: #333; 
    margin: 0;
    padding: 20px;
}

form {
    max-width: 500px;
    margin: 50px auto;
    padding: 20px;
    background-color: #ffffff; 
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2); 
    border-radius: 10px;
}

form h1 {
    text-align: center;
    color: #0078d7; 
    margin-bottom: 20px;
    font-size: 2rem;
}

form input {
    display: block;
    width: calc(100% - 20px);
    margin: 10px auto;
    padding: 10px;
    font-size: 1rem;
    border: 2px solid #0078d7; 
    border-radius: 5px;
    box-sizing: border-box;
}
form input:focus {
    outline: none;
    border-color: #005bb5;
    box-shadow: 0 0 5px rgba(0, 123, 215, 0.5); 
}
form button {
    display: block;
    width: 100%;
    padding: 10px;
    background-color: #0078d7; 
    color: #ffffff;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    font-size: 1rem;
    transition: background-color 0.3s;
}

form button:hover {
    background-color: #005bb5; 
}
</style>
<body>
    <form action="/updateData" method="post">
        <h1 style="text-align: center;">Edit</h1>
        <input type="text" value="<%= singleData.id %>" hidden name="id">
        <input type="text" value="<%= singleData.Name %>" placeholder=" Name" name="book">
        <input type="text" value="<%= singleData.author %>" placeholder="Author" name="author">
        <input type="number" value="<%= singleData.Price %>" placeholder="Price" name="price">
        <input type="number" value="<%= singleData.ryear %>" placeholder="ryear" name="ryear">
        <button type="submit">Update</button>
    </form>
</body>
</html>